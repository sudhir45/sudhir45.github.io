---
// Reading-view article body. Typography (drop cap, amber blockquote rule,
// Space Grotesk subheads) lives in the `.reading` layer in global.css;
// `.content` keeps the rehype-pretty-code block styling hooks.
---

<article class="content reading mx-auto max-w-[800px]">
	<slot />
</article>
