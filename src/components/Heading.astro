---
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<h1 class="mb-8 text-center text-3xl font-semibold tracking-tight text-stone-900 dark:text-stone-100 md:text-4xl">{title}</h1>
